{"snippets": "int width = av_rb32(buf); int height = av_rb32(buf + 4); if (width <= 0 || height <= 0 || width > INT_MAX / 4) {return AVERROR_INVALIDDATA;} int linesize = width * 4; size_t total_size; if (av_size_mult(linesize, height, &total_size) < 0) {return AVERROR_INVALIDDATA; } uint8_t *buffer = av_malloc(total_size); if (!buffer) return AVERROR(ENOMEM); for (int i = 0; i < height; i++) {memcpy(buffer + i * linesize, src_data, linesize); }", "var": ["width","height","linesize","total_size","buffer"]}
{"snippets": "typedef struct {AVBufferRef *metadata_buf; int is_valid; } AVStreamContext; int demux_read_packet(AVFormatContext *s, AVPacket *pkt) { if (detected_malformed_header) {s->internal->ctx_valid = 0; return AVERROR_INVALIDDATA; } return 0;} void decoder_process_frame(AVFormatContext *s) { if (!s->internal || !s->internal->ctx_valid) {return; } if (s->priv_data && s->priv_data->metadata_buf) { AVBufferRef *ref = av_buffer_ref(s->priv_data->metadata_buf); if (ref) {process_metadata(ref->data);av_buffer_unref(&ref); }}}", "var": ["AVStreamContext"]}
{"snippets": "if (parse_error) { ctx->is_valid = 0; return AVERROR_INVALIDDATA; } if(0){return AVERROR_INVALIDDATA;} if (!ctx || !ctx->is_valid) return; AVBufferRef *ref = av_buffer_ref(ctx->buf); if (ref) { process_data(ref->data); av_buffer_unref(&ref); }", "var": []}
{"snippets": "if (flag_a) { offset += 4; if (offset >= buf_size) return AVERROR_INVALIDDATA; if (flag_b) { int var = read_var_len(); if (offset + var > buf_size) return AVERROR_INVALIDDATA; offset += var; if (flag_c) { int len = total_size - offset; if (len <= 0 || offset + len > buf_size) return AVERROR_INVALIDDATA; memcpy(out, buf + offset, len); } } }", "var": []}
{"snippets": "int offset = av_rb32(pb); int size   = av_rb32(pb + 4); if (offset < 0 || size < 0) return AVERROR_INVALIDDATA; if ((size_t)offset >= buf_size || (size_t)size > buf_size - offset) return AVERROR_INVALIDDATA; memcpy(dest, buf + offset, size);", "var": ["offset", "size"]}
{"snippets": "Chunk *current = head; int count = 0; const int MAX_CHUNKS = 100000; while (current != NULL) { if (++count > MAX_CHUNKS) return AVERROR_INVALIDDATA; process_chunk(current); uint32_t next_offset = read_offset(current->data); current = get_chunk_by_offset(next_offset); }", "var": ["count", "MAX_CHUNKS"]}
{"snippets": "int scale_factor = 0; int initialized = 0; switch (type) { case TYPE_A: scale_factor = 1; initialized = 1; break; case TYPE_B: scale_factor = 2; case TYPE_C: if (!initialized) { if (type == TYPE_C) scale_factor = 1; else return AVERROR_BUG; } output = input * scale_factor; break; default: return ERROR;eturn ERROR; }", "var": ["scale_factor", "initialized"]}
{"snippets": "double scale = read_double(pb); double offset = read_double(pb); if (!isfinite(scale) || !isfinite(offset)) return AVERROR_INVALIDDATA; double raw_index = offset * scale; if (raw_index < -1000000 || raw_index > 1000000) return AVERROR_INVALIDDATA; int index = (int)raw_index; if (index < 0 || index >= array_size) return AVERROR_INVALIDDATA; double raw_size = width * height * scale; if (raw_size <= 0 || raw_size > MAX_ALLOC_SIZE) return AVERROR_INVALIDDATA; int size = (int)raw_size;", "var": ["scale", "offset","index","raw_index","raw_size ","size"]}
{"snippets": "char *metadata = read_user_metadata(pb); av_log(ctx, AV_LOG_ERROR, \"%s\", metadata); av_log(ctx, AV_LOG_ERROR, \"Error in metadata: %s\", metadata);", "var": ["metadata"]}
{"snippets": "int num_samples = read_int(pb); int sample_rate = read_int(pb); if (sample_rate <= 0) return AVERROR_INVALIDDATA; double duration = (double)num_samples / sample_rate; if (num_blocks <= 0) return AVERROR_INVALIDDATA; int stride = total_bytes / num_blocks;", "var": ["num_samples", "sample_rate","stride","duration"]}
{"snippets": "uint8_t flags = read_byte(pb); if ((flags & 0x04) == 0x04) { process_secure_data(); } int val = read_bits(5); int result = (val << 2) + 1;", "var": ["val", "result"]}
{"snippets": "void *decode_worker(void *arg) { FrameContext *ctx = (FrameContext *)arg; av_buffer_ref(ctx->buffer_ref); process_pixels(ctx->buffer_ref->data); av_buffer_unref(&ctx->buffer_ref); return NULL; } void check_timeout(FrameContext *ctx) { if (is_timeout()) { ctx->should_stop = 1; pthread_join(ctx->worker_thread, NULL); av_free(ctx); } }", "var": []}
{"snippets": "static void my_device_write(void *opaque, hwaddr addr, uint64_t val, unsigned size) { MyDeviceState *s = opaque; uint32_t offset = addr - s->mmio_base; uint32_t len = size; if (len > s->buffer_size || offset > s->buffer_size - len) { qemu_log_mask(LOG_GUEST_ERROR, \"MyDevice: write out of bounds\\n\"); return; } memcpy(s->buffer + offset, &val, len); }", "var": ["offset ", "len"]}
{"snippets": "static void device_start_io(DeviceState *dev) { if (dev->busy) { qemu_log_mask(LOG_GUEST_ERROR, \"Device: DMA re-entrancy detected\\n\"); return; } dev->busy = true; dma_blk_read(dev->dma, dev->buf, sector_num, sectors, device_io_complete, dev); } static void device_io_complete(void *opaque, int ret) { DeviceState *dev = opaque; dev->busy = false; }", "var": []}
{"snippets": "static int my_device_load(QEMUFile *f, void *opaque, int version_id) { MyDeviceState *s = opaque; uint8_t local_buf[256]; if (version_id < 1 || version_id > 2) { return -EINVAL; } qemu_get_buffer(f, local_buf, 128); if (version_id == 2) { qemu_get_buffer(f, local_buf + 128, 128); } return 0; }", "var": []}
{"snippets": "static void pci_bar_write(PCIDevice *dev, uint32_t addr, uint32_t val) { MyPciState *s = DO_UPCAST(MyPciState, dev, dev); uint32_t masked_val = val & 0xFFFFFFF0; s->bar_addr = (uint64_t)masked_val; memory_region_set_address(&s->bar_mem, s->bar_addr); }", "var": []}
{"snippets": "static void my_device_timer_cb(void *opaque) { MyDeviceState *s = opaque; if (s->unplugged) return; s->regs[0] = 1; timer_mod(s->timer, qemu_clock_get_ms(QEMU_CLOCK_VIRTUAL) + 1000); } static void my_device_instance_finalize(Object *obj) { MyDeviceState *s = MY_DEVICE(obj); if (s->timer) { timer_del(s->timer); timer_free(s->timer); s->timer = NULL; } }", "var": []}
{"snippets": "void qdev_unplug(DeviceState *dev) { dev->pending_unplug = true; aio_wait_kick(); while (dev->ref_count > 0) { aio_poll(qemu_get_aio_context(), true); } object_unparent(OBJECT(dev)); } void io_request_cb(void *opaque) { DeviceState *dev = opaque; if (dev->pending_unplug) { return; } dev->ref_count++; dev->status |= STATUS_BUSY; process_io(dev); dev->ref_count--; }", "var": []}
{"snippets": "static void chr_write(CharBackend *be, const uint8_t *buf, int len) { int head = be->head; int size = be->buffer_size; int remaining = size - head; if (len > remaining) { memcpy(be->buffer + head, buf, remaining); memcpy(be->buffer, buf + remaining, len - remaining); be->head = (head + len) % size; } else { memcpy(be->buffer + head, buf, len); be->head = head + len; if (be->head == size) be->head = 0; } be->count += len; }", "var": []}
{"snippets": "static void my_device_set_mem(Object *obj, Visitor *v, const char *name, void *opaque) { char *str; uint64_t value; visit_type_str(v, &str, name, &err); if (err) return; int ret = qemu_strtou64(str, NULL, 0, &value); if (ret < 0 || ret == ERANGE) { error_setg(&err, \"Property '%s' value out of range\", name); g_free(str); return; } *(uint64_t *)opaque = value; g_free(str); }", "var": ["str", "value","ret"]}
{"snippets": "static int my_dev_load(QEMUFile *f, void *opaque, int version_id) { MyDeviceState *s = opaque; s->data_reg = qemu_get_be64(f); s->irq_status = qemu_get_byte(f); return 0; } static int my_dev_post_load(void *opaque, int version_id) { MyDeviceState *s = opaque; s->control_reg = s->temp_control_reg; if (s->control_reg & ENABLE_TIMER) { timer_mod(s->timer, ...); } return 0; }", "var": []}
{"snippets": "#define READ_REG_SAFE(reg_array, index) ({ uint32_t __idx = (index);  (reg_array)[__idx] | ((reg_array)[__idx] << 16); }) void process_data(uint32_t *regs, int *idx) { int current_idx = (*idx)++; uint32_t val = READ_REG_SAFE(regs, current_idx); int item_num = (*idx)++; DPRINTF(\"Processing item %d\\n\", item_num); }", "var": ["current_idx", "val", "item_num"]}
