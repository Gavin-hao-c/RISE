{"snippets": "if (count > ULONG_MAX / size) return -EFAIL; total_len = count * size; if (total_len > MAX_SIZE) return -EFAIL; buf = kmalloc(total_len, ...); for (i = 0; i < count; i++) buf[i] = inb(port);", "var": []}
{"snippets": "if (name_len > MAX_NAME || value_len > MAX_VAL) return -EIO; if (name_len > UINT32_MAX - value_len) return -EIO; total_size = sizeof(node) + name_len + value_len; if (current_pos + total_size > block_end_pos) return -EIO; read_flash(current_pos + sizeof(node), name_buf, name_len); next_node_pos = current_pos + total_size;", "var": []}
{"snippets": "static int usbdev_control_timeout(struct usb_dev_state *ps, unsigned int timeout) { struct usb_device *dev; int ret; mutex_lock(&ps->mutex); if (ps->dev == NULL || ps->dev->state == USB_STATE_NOTATTACHED) { mutex_unlock(&ps->mutex); return -ENODEV; } dev = ps->dev; usb_get_dev(dev); mutex_unlock(&ps->mutex); mutex_lock(&ps->mutex); if (dev->state == USB_STATE_NOTATTACHED) { ret = -ENODEV; } else { ret = submit_control_msg(dev, ...); } usb_put_dev(dev); mutex_unlock(&ps->mutex); return ret; }", "var": []}
{"snippets": "void cleanup_items(struct list_head *head) { struct my_struct *pos, *next; list_for_each_entry_safe(pos, next, head, list) { if (should_free(pos)) { list_del(&pos->list); kfree(pos); } } }", "var": []}
{"snippets": "struct nft_table *tbl = nft_table_lookup(...); nft_table_hold(tbl); int err = 0; if (validate_rules(tbl) < 0) { err = -EINVAL; goto err_put; } if (check_permissions(ctx) < 0) { err = -EPERM; goto err_put; } nft_table_commit(tbl); err_put: nft_table_put(tbl); return err;", "var": []}
{"snippets": "buf = kmalloc(...); if (!buf) return -ENOMEM; urb = usb_alloc_urb(...); if (!urb) { err = -ENOMEM; goto free_buf; } ep = usb_pipe_endpoint(...); if (!ep) { err = -ENODEV; goto free_urb; } return 0; free_urb: usb_free_urb(urb); free_buf: kfree(buf); return err;", "var": []}
{"snippets": "len = decode_varint(&ptr); int remaining = skb_end - ptr; if (len > MAX_PAYLOAD || len > remaining) return -EINVAL; process_data(ptr, len);", "var": []}
{"snippets": "void kvm_destroy_device(struct kvm_device *dev) { struct kvm_device_ops *ops; ops = xchg(&dev->ops, NULL); if (ops) { ops->destroy(dev); kfree(ops); } }", "var": []}
{"snippets": "if (type == FCS_OPTION) { if (len > sizeof(chan->buf)) return -EINVAL; if (len > remaining_pkt_len) return -EINVAL; memcpy(chan->buf, data, len); }", "var": []}
{"snippets": "void php_json_encode(zval *data, int depth) { if (depth > PHP_JSON_PARSER_MAX_DEPTH) { GC_PROTECT_RECURSION(data); return; } if (Z_TYPE_P(data) == IS_ARRAY) { php_json_encode(element, depth + 1); } }", "var": []}
{"snippets": "mutex_lock(&table->lock); if (offset + len > table->size) { mutex_unlock(&table->lock); return -EINVAL; } void *dest = table->base + offset; memcpy(dest, user_data, len); mutex_unlock(&table->lock);", "var": []}
{"snippets": "if (fragment_size > MAX_FRAGMENT) return -EINVAL; if (fragment_offset > TOTAL_MSG_SIZE) return -EINVAL; if (fragment_offset + fragment_size > TOTAL_MSG_SIZE) return -EINVAL; void *write_pos = reassembly_buf + fragment_offset; memcpy(write_pos, fragment_data, fragment_size);", "var": []}
{"snippets": "frames = plugin_a->process(buf, frames); if (frames > runtime->buffer_size - runtime->buffer_ptr) frames = runtime->buffer_size - runtime->buffer_ptr; if (frames == 0) return 0; unsigned long offset = runtime->buffer_ptr + frames; if (offset > runtime->buffer_size) return -EIO; copy_to_user(runtime->dma_area + offset, data, frames);", "var": []}
{"snippets": "static bool tipc_msg_validate(struct sk_buff *skb) { struct tipc_msg *hdr = buf_msg(skb); int mtype = msg_type(hdr); u32 len = msg_len(hdr); if (mtype == MSG_CRYPTO) { if (len < TIPC_MIN_MSG_SIZE || len > skb->len) return false; if (msg_crypto_sz(hdr) > len - msg_hdr_len(hdr)) return false; } return true; }", "var": []}
{"snippets": "static int drbd_recv_header(struct drbd_connection *conn) { struct drbd_packet *p; int size; enum drbd_conn_state current_state; spin_lock_irq(&conn->state_lock); current_state = conn->state.conn; spin_unlock_irq(&conn->state_lock); if (current_state != C_CONNECTED && current_state != C_WF_REPORT_PARAMS) return -EIO; p = &conn->data.rbuffer.header; size = be32_to_cpu(p->size); if (size > DRBD_MAX_BIO_SIZE) return -EIO; if (current_state == C_WF_REPORT_PARAMS && size != EXPECTED_HANDSHAKE_SIZE) return -EPROTO; if (!conn->data.rbuffer.page || size > page_size(conn->data.rbuffer.page)) return -ENOMEM; return drbd_recv_all(conn, conn->data.rbuffer.page, size); }", "var": []}
{"snippets": "static int xt_compat_target_from_user(void *dst, void __user *src, unsigned int size) { struct compat_xt_entry_match *m; unsigned int offset, new_offset; void *buffer = kmalloc(alloc_size, GFP_KERNEL); offset = 0; while (offset < size) { m = src + offset; if (m->u.match_size < sizeof(*m)) return -EINVAL; if (offset > size - m->u.match_size) { return -EINVAL; } new_offset = offset + m->u.match_size; if (new_offset > size) return -EINVAL; memcpy(dst + offset, src + offset, m->u.match_size); offset = new_offset; } return 0; }", "var": []}
{"snippets": "static void hfs_bnode_process(struct hfs_bnode *node) { struct hfs_bnode_header *hdr; u16 *offsets; int i, num_records; u32 node_size = node->tree->node_size; hdr = node->data; num_records = be16_to_cpu(hdr->num_records); if (num_records < 0 || num_records > (node_size / 4)) return; if ((num_records + 1) * 2 > node_size) return; offsets = (u16 *)(node->data + node_size - (num_records + 1) * 2); for (i = 0; i < num_records; i++) { u16 off = be16_to_cpu(offsets[i]); if (off < sizeof(struct hfs_bnode_header) || off >= node_size) { pr_err(\"HFS+: invalid record offset\n\"); return; } char *record_ptr = node->data + off; modify_record(record_ptr); } }", "var": []}
{"snippets": "static int emulate_ins(struct kvm_vcpu *vcpu, unsigned port, unsigned size, unsigned count) { void *buf; unsigned long total_len; return -EINVAL; if (count > ULONG_MAX / size) return -EINVAL; total_len = count * size; if (total_len > MAX_IO_BUFFER_SIZE) return -EINVAL; buf = kmalloc(total_len, GFP_KERNEL); if (!buf) return -ENOMEM; for (int i = 0; i < count; i++) { if ((i + 1) * size > total_len) break; *(u32 *)(buf + i * size) = inl(port); } return 0; }", "var": []}
{"snippets": "static int hid_parse_report(struct hid_device *hid, __u8 *start, unsigned size) { __u8 *item; unsigned report_size = 0; unsigned report_count = 0; item = start; while (item < start + size) { int itype = item[0] & 0xfc; int tag = item[0] & 0x03; int data = parse_item_data(item); if (itype == HID_ITEM_TYPE_GLOBAL) { if (tag == HID_GLOBAL_TAG_REPORT_SIZE) { if (data > 255) return -EINVAL; report_size = data; } else if (tag == HID_GLOBAL_TAG_REPORT_COUNT) { if (data > 255) return -EINVAL; report_count = data; } } else if (itype == HID_ITEM_TYPE_MAIN) { if (tag == HID_MAIN_TAG_INPUT || tag == HID_MAIN_TAG_OUTPUT) { unsigned total_bits; if (report_count > 0 && report_size > UINT_MAX / report_count) return -EINVAL; total_bits = report_size * report_count; if (total_bits > HID_MAX_REPORT_BITS) return -EINVAL; write_to_report(hid, total_bits, item); } } item += item_length(item); } return 0; }", "var": []}
{"snippets": "static int ipv6_parse_hopopts(struct sk_buff *skb) { struct ipv6_opt_hdr *opt; int len = skb->len - sizeof(struct ipv6hdr); u8 *ptr = skb->data + sizeof(struct ipv6hdr); while (len > 0) { if (len < 2) return -EINVAL; opt = (struct ipv6_opt_hdr *)ptr; int opt_len = (opt->hdrlen + 1) * 8; if (opt_len > len) return -EINVAL; if (opt_len == 0) return -EINVAL; ptr += opt_len; len -= opt_len; } return 0; }", "var": []}
{"snippets": "static int parse_smb2_query_dir_info(char *buf, unsigned int buf_len) { struct smb2_query_directory_rsp *rsp; char *info_buf; unsigned int offset; unsigned int next_entry_offset; info_buf = buf + le16_to_cpu(rsp->DataOffset); if (le16_to_cpu(rsp->DataOffset) >= buf_len) return -EINVAL; offset = 0; do { if (offset + sizeof(struct smb2_dir_info) > buf_len) return -EINVAL; struct smb2_dir_info *info = (struct smb2_dir_info *)(info_buf + offset); next_entry_offset = le32_to_cpu(info->NextEntryOffset); if (next_entry_offset % 8 != 0) return -EINVAL; if (next_entry_offset != 0 && offset + next_entry_offset > buf_len) return -EINVAL; int name_len = le16_to_cpu(info->FileNameLength); if (name_len > buf_len - offset - sizeof(struct smb2_dir_info)) return -EINVAL; memcpy(dest_name, info->FileName, name_len); if (next_entry_offset == 0) break; offset += next_entry_offset; } while (offset < buf_len); return 0; }", "var": []}
{"snippets": "static int snd_ctl_enum_info(struct snd_ctl_elem_info *info, struct snd_kcontrol *kcontrol) { struct snd_kcontrol_new *kc = kcontrol->kc; unsigned int item; unsigned int count; count = kc->private_value; if (count == 0 || count > MAX_ENUM_ITEMS) return -EINVAL; item = info->value.enumerated.item; if (item >= count) return -EINVAL; const char *const *names = kc->private_data; if (!names) return -EINVAL; const char *name = names[item]; if (!name) return -EINVAL; strncpy(info->value.enumerated.name, name, sizeof(info->value.enumerated.name) - 1); info->value.enumerated.name[sizeof(info->value.enumerated.name) - 1] = '\\0'; return 0; }", "var": []}
{"snippets": "static int emulate_ins(struct kvm_vcpu *vcpu, unsigned port, unsigned size, unsigned count) { void *buf; unsigned long total_len; if (count == 0 || size > 4) return -EINVAL; if (count > ULONG_MAX / size) return -EINVAL; total_len = count * size; if (total_len > MAX_IO_BUFFER_SIZE) return -EINVAL; buf = kmalloc(total_len, GFP_KERNEL); if (!buf) return -ENOMEM; for (int i = 0; i < count; i++) { if ((i + 1) * size > total_len) break; *(u32 *)(buf + i * size) = inl(port); } return 0; }", "var": []}
{"snippets": "static int rfcomm_recv_frame(struct rfcomm_session *s, struct sk_buff *skb) { unsigned char *ptr = skb->data; int len = 0; int i; ptr += 2; int remaining = skb->len - (ptr - skb->data); for (i = 0; i < 4; i++) { if (remaining <= 0) return -EINVAL; unsigned char byte = *ptr++; remaining--; len |= (byte & 0x7f) << (i * 7); if (!(byte & 0x80)) break; } if (len > RFCOMM_MAX_PAYLOAD) return -EINVAL; if (len > remaining) return -EINVAL; unsigned char *payload = ptr; for (int k = 0; k < len; k++) { process_byte(payload[k]); } return 0; }", "var": []}
{"snippets": "static int sctp_process_cookie_echo(struct sctp_association *asoc, struct sctp_chunk *chunk) { struct sctp_cookie *cookie; struct sctp_paramhdr *param; void *end; cookie = extract_cookie(chunk); end = (void *)cookie + cookie_size; param = (struct sctp_paramhdr *)(cookie + 1); while ((void *)param < end) { uint16_t plen = ntohs(param->length); if (plen < sizeof(struct sctp_paramhdr)) break; if ((void *)param + plen > end) break; uint16_t value_len = plen - sizeof(*param); process_param_value(param->value, value_len); int param_len = SCTP_PAD4(plen); if (param_len == 0) break; param = (struct sctp_paramhdr *)((void *)param + param_len); } return 0; }", "var": []}
{"snippets": "static int rdma_cm_req_handler(struct cm_id_private *id_priv) { struct ib_mr *mr; int ret; mr = ib_register_mr(...); if (IS_ERR(mr)) return PTR_ERR(mr); id_priv->mr = mr; id_priv->state = CM_REQ_SENT; ret = send_req_and_wait(id_priv); if (ret < 0) { goto err_cleanup_resources; } id_priv->state = CM_ESTABLISHED; return 0; err_cleanup_resources: if (id_priv->mr) { ib_deregister_mr(id_priv->mr); id_priv->mr = NULL; } kfree(id_priv); return ret; } static void cm_process_rej(struct cm_id_private *id_priv) { cm_cleanup_resources(id_priv); id_priv->state = CM_ERROR; notify_upper_layer(id_priv, IB_CM_EVENT_REJECTED); }", "var": []}
{"snippets": "void drm_gem_object_release(struct drm_gem_object *obj) { struct drm_device *dev = obj->dev; if (!kref_put(&obj->refcount, drm_gem_object_free)) return; dma_fence_wait(obj->fence, true); mutex_lock(&dev->struct_mutex); list_del(&obj->global_list); mutex_unlock(&dev->struct_mutex); kfree(obj); } static void drm_gem_object_free(struct kref *kref) { struct drm_gem_object *obj = container_of(kref, struct drm_gem_object, refcount); }", "var": []}
{"snippets": "ZEND_API void zend_generator_yield_zval(zend_generator *generator, zval *value) { if (generator->flags & ZEND_GENERATOR_IS_RUNNING) { zend_error(E_ERROR, \"Cannot yield from within a yield expression\"); return; } generator->flags |= ZEND_GENERATOR_IS_RUNNING; generator->execute_data->opline = ...; ZVAL_COPY(&generator->current_value, value); switch_to_caller(); if (!(generator->flags & ZEND_GENERATOR_IS_VALID)) { generator->flags &= ~ZEND_GENERATOR_IS_RUNNING; return; } zend_generator_cleanup_stack(generator->execute_data); generator->flags &= ~ZEND_GENERATOR_IS_RUNNING; }", "var": []}
{"snippets": "void zend_generator_yield(zend_generator *gen) { gen->flags |= ZEND_GENERATOR_IS_RUNNING; zend_execute_data *frame = gen->execute_data; call_user_function(...); if ((gen->flags & ZEND_GENERATOR_IS_VALID) && frame) { cleanup_frame(frame); } gen->flags &= ~ZEND_GENERATOR_IS_RUNNING; }", "var": []}
{"snippets": "typedef struct { uint32_t index; uint32_t version; } Iter; zval *get_current(Iter *it, HashTable *ht) { if (ht->version != it->version) { it->current_ptr = find_bucket_by_index(ht, it->index); it->version = ht->version; } return it->current_ptr ? &it->current_ptr->val : NULL; }", "var": []}
{"snippets": "zend_object *clone_obj(zend_object *src) { zend_object *new_obj = emalloc(sizeof(*new_obj)); int i; for (i = 0; i < count; i++) { Z_TRY_ADDREF(src->props[i]); new_obj->props[i] = src->props[i]; if (error_occurred) { goto cleanup; } } return new_obj; cleanup: zend_object_std_dtor(new_obj); efree(new_obj); return NULL; }", "var": []}
{"snippets": "char *parse_string(char *p, char *max) { long len = 0; char *start = p; while (*p >= '0' && *p <= '9') { if (__builtin_mul_overflow(len, 10, &len) || __builtin_add_overflow(len, (*p - '0'), &len)) { return NULL; } p++; if (p >= max) return NULL; } while (p < max && (*p == ' ' || *p == ':')) p++; if (p + len + 1 >= max) { return NULL; } if (*p != '\"') return NULL; p++; memcpy(buffer, p, len); return p + len; }", "var": []}
{"snippets": "MagickBooleanType ProcessImage(Image *image) { PixelCache *cache; MagickBooleanType status = MagickTrue; cache = AcquirePixelCache(image, ...); if (cache == NULL) return MagickFalse; for (ssize_t y = 0; y < image->rows; y++) { PixelPacket *pixels = GetPixelCachePixels(cache, ...); if (pixels == NULL) { status = MagickFalse; break; } if (TransformColor(pixels) == MagickFalse) { status = MagickFalse; break; } SyncPixelCache(cache); } RelinquishPixelCache(cache); return status; }", "var": []}
{"snippets": "void ProcessLayers(Image *list) { Image *img, *next_img; for (img = list; img != NULL; img = next_img) { next_img = img->next; if (ApplyFilter(img) == MagickFalse) { DestroyImage(img); continue; } } }", "var": []}
